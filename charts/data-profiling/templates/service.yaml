apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-profiling.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "data-profiling.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-profiling.selectorLabels" . | nindent 4 }}
